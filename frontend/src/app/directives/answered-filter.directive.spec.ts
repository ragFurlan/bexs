import { AnsweredFilterDirective } from './answered-filter.directive';

describe('AnsweredFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new AnsweredFilterDirective();
    expect(directive).toBeTruthy();
  });
});
